---
import Layout from "../../layouts/Layout.astro";

if (!import.meta.env.SSR) {
  const token = localStorage.getItem("admin_token");
  if (!token) {
    window.location.href = "/admin/login";
  }
}
---

<Layout title="Články">
  <h1>Články</h1>
  <p>Seznam všech článků na webu.</p>
</Layout>
